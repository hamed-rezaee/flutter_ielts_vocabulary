name: flutter_ielts_vocabulary
description: IELTS Vocabulary project.

publish_to: "none"

version: 1.0.1+2

environment:
  sdk: ">=2.9.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  moor_flutter: ^3.1.0
  provider: ^4.3.2
  flutter_tts: ^1.2.6
  file_picker: ^1.13.2

  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  moor_generator: ^3.3.1
  build_runner: null

flutter:
  uses-material-design: true
